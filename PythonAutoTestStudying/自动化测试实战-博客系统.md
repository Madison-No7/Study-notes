博客系统地址：http://47.108.157.13:8090/blog_login.html
# 一、设计自动化测试用例
使用思维导图来编写测试用例:[[博客系统自动化测试用例设计.canvas]]


# 二、编写自动化测试脚本
## 1)创建自动化测试项目
新建一个python项目用于自动化测试项目
  ![[Pasted image 20260813180321.png]]
  ![[Pasted image 20260813180355.png]]
创建好项目后，需要在项目设置中查看是否下载了selenium和webdriver-manager库。
![[Pasted image 20260813180622.png]]
![[Pasted image 20260813180648.png]]
![[Pasted image 20260813180740.png]]
![[Pasted image 20260813180819.png]]
至此，自动化测试Python空项目创建完毕。
![[Pasted image 20260813181159.png]]
由于我们测试该博客系统主要测试有四个部分，分别是博客登录页、博客首页、博客详情页、博客编辑页。所以我们创建4个Python文件，每一个文件写对应页面的自动化测试脚本。
**分别是**：
``BlogLoginTest.py ``     博客登录页测试用例
``BlogListTest.py``         博客首页测试用例
``BlogDetailTest.py``     博客详情页测试用例
``BlogEditTest.py``         博客编辑页测试用例
由于在每一个测试脚本中，都要创建一个浏览器驱动对象，如果有几百个页面需要测试，岂不是需要重复创建几百个浏览器驱动对象，那也太麻烦了吧。那怎么可以变得简便一些呢？
我们可以在一个单独的Python的文件中创建一个浏览器驱动对象的类，再实例化这个类，因为模块天然支持单例模式，浏览器驱动对象在``Utils.py``模块中已经创建了，以模块的方式导入到其他模块即可使用，全程就只创建了一个驱动对象了。
因此，总体项目路径可以设计为：
``common:``
	``Utills.py``
``test:``
	``BlogLoginTest.py ``     
	``BlogListTest.py``         
	``BlogDetailTest.py``     
	``BlogEditTest.py``         
好的，我们开始创建项目：
![[Pasted image 20260815155521.png]]
在项目目录下，新建一个[Python软件包](Python包是什么？.md)，然后在软件包路径下，新建上述Python文件。
![[Pasted image 20260815155839.png]]
## 2）自动化测试脚本编写
### **Utils.py:**
这个py文件的目的是创建一个Chrome浏览器驱动对象类，设置好浏览器的启动配置（options）以及对应版本的驱动程序。最后要将创建的类实例化，实现全局只创建一个浏览器驱动对象。
```
#创建浏览器对象  
from selenium import webdriver  
from selenium.webdriver.ie.service import Service  
from webdriver_manager.chrome import ChromeDriverManager  
from selenium.webdriver.support.wait import WebDriverWait
  
class Driver:  
    driver=''
    #显示等待器  
	wait=''  
    #实例化Options对象  
    Options=webdriver.ChromeOptions()  
    # 1. 禁用 Blink 自动化特征  
    Options.add_argument('--disable-blink-features=AutomationControlled')  
  
    # 2. 移除顶部控制提示条  
    Options.add_experimental_option("excludeSwitches", ["enable-automation"])  
    Options.add_experimental_option('useAutomationExtension', False)  
  
    # 3. 传入真实的 User-Agent    Options.add_argument(  
        "user-agent=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/151.0.0.0 Safari/537.36")  
   
    # 彻底关闭 Chrome 密码管理器 & 密码泄露检测弹窗  
    prefs = {  
        # 1. 禁用 Chrome 询问是否保存密码的弹窗  
        "credentials_enable_service": False,  
        "profile.password_manager_enabled": False,  
        # 2. 禁用图片中弹出的“密码遭遇数据泄露”警告弹窗  
        "profile.password_manager_leak_detection": False  
    }  
    Options.add_experimental_option("prefs", prefs)  
  
    #添加页面加载策略  
    Options.page_load_strategy='eager'  
  
    #构造函数  
    def __init__(self):  
	self.driver=webdriver.Chrome(service=Service(ChromeDriverManager().install()),
	options=Driver.Options) 
	self.wait=WebDriverWait(self.driver, timeout=10) 
  
#实例化Chrome浏览器驱动对象  
ChromeDriver=Driver()
```
### 增加屏幕截图的功能：
有时候测试没有通过，程序只会报一个简单没有通过测试，无法追溯错误的根源，因此，在我们编写自动化测试脚本的时候，每执行一段测试，保存上测试页面的截图，当测试报错的时候，便一目了然了。
为了更好的根据屏幕截图定位错误的根源，我们要创建一个images文件夹,专门用于存放截图。图片名称的设计也有讲究，图片命名：``调用的方法名-年-月-日-时分秒.png``，便于追溯错误根源。因为在每一个测试环节都要用到屏幕截图功能，所以我们可以将屏幕截图封装成一个方法，作为公共类（Driver）的实例方法，之后在其他模块直接根据Driver类实例对象调用即可。
**注意：**
获取调用当前函数的上一层函数的名称的方法：[sys._getframe(1).f_code.co_name](PythonAutoTestStudying/获取调用的方法名的函数)
```

```
### **BlogLoginTest.py：**  

**此文件的目的:**
是按照设计的登录页测试用例编写自动化测试脚本。
```
from selenium.webdriver.common.by import By  
#通过模块导入Chrome驱动对象  
from common.Utils import ChromeDriver  
from selenium.webdriver.support.wait import WebDriverWait  
from selenium.webdriver.support import expected_conditions as EC  
  
#测试登录页面  
class Login:  
    driver=''  
    wait=''  
    def __init__(self):  
        #实例驱动对象  
        self.driver=ChromeDriver.driver  
        #创建等待器,最长等待10秒  
        self.wait=WebDriverWait(self.driver, timeout=10)  
  
    #测试成功登录  
    def succ_login(self):  
        self.driver.get('http://47.108.157.13:8090/blog_login.html')  
        #指定等待条件  
        input_name=self.wait.until(EC.visibility_of_element_located((By.XPATH,'//*[@id="username"]')))  
        input_name.send_keys('zhangsan')  
  
        input_pwd=self.wait.until(EC.visibility_of_element_located((By.XPATH,'//*[@id="password"]')))  
        input_pwd.send_keys('123456')  
  
        button=self.wait.until(EC.visibility_of_element_located((By.XPATH,'//*[@id="submit"]')))  
        button.click()  
  
        name_element= input_pwd=self.wait.until(EC.visibility_of_element_located((By.XPATH,'/html/body/div[2]/div[1]/div/h3')))  
        name=name_element.text  
        if name == 'zhangsan':  
            print('成功登录-测试结束')  
        else:  
            print('成功登录-测试失败')  
  
  
    #测试失败登录--三种情况  
    def fail_login(self):  
        self.driver.get('http://47.108.157.13:8090/blog_login.html')  
        #正确的账号，错误的密码  
        input_name = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="username"]')))  
        input_name.send_keys('zhangsan')  
  
        input_pwd = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="password"]')))  
        input_pwd.send_keys('12345')  
  
        button = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="submit"]')))  
        button.click()  
  
        #出现密码不正确弹窗  
        alert=self.wait.until(EC.alert_is_present())  
        alert_text=alert.text  
        if alert_text == '密码不正确':  
            print('登录失败-测试成功-正确的账号，错误的密码')  
        else:  
            print('登录失败-测试成功-正确的账号，错误的密码')  
  
        alert.accept()  
  
        # 错误的账号，错误的密码  
        #清空输入框中的密码  
        input_name = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="username"]')))  
        input_name.clear()  
        input_name.send_keys('lisi')  
  
        input_pwd = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="password"]')))  
        input_pwd.clear()  
        input_pwd.send_keys('12345')  
  
        button = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="submit"]')))  
        button.click()  
  
        # 出现密码不正确弹窗  
        alert = self.wait.until(EC.alert_is_present())  
        alert_text = alert.text  
        if alert_text == '密码不正确':  
            print('登录失败-测试成功-错误的账号，错误的密码')  
        else:  
            print('登录失败-测试成功-错误的账号，错误的密码')  
  
        alert.accept()  
  
        # 错误的账号，正确的密码  
        # 清空输入框中的密码  
        input_name = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="username"]')))  
        input_name.clear()  
        input_name.send_keys('lisii')  
  
        input_pwd = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="password"]')))  
        input_pwd.clear()  
        input_pwd.send_keys('123456')  
  
        button = self.wait.until(EC.visibility_of_element_located((By.XPATH, '//*[@id="submit"]')))  
        button.click()  
  
        # 出现密码不正确弹窗  
        alert = self.wait.until(EC.alert_is_present())  
        alert_text = alert.text  
        if alert_text == '用户不存在':  
            print('登录失败-测试成功-错误的账号，正确的密码')  
        else:  
            print('登录失败-测试成功-错误的账号，正确的密码')  
        alert.accept()  

```




